# SpellCode Voice AI - Python Dependencies
# Core dependencies for STT, TTS, and state management

# Deepgram SDK for STT and TTS
deepgram-sdk>=5.0.0   # v5+ required — AsyncDeepgramClient only exists from v5 onwards

# Async HTTP client
httpx>=0.25.0
aiohttp>=3.9.0

# WebSocket support
websockets>=11.0

# API and Web Framework
fastapi>=0.100.0
uvicorn>=0.23.0

# Audio processing
pyaudio>=0.2.14
numpy>=1.24.0
sounddevice>=0.4.6

# Wake word detection (ONNX Runtime - lightweight, no PyTorch)
onnxruntime>=1.16.0
librosa>=0.10.0

# State management and async
pydantic>=2.5.0
pydantic-settings>=2.3.0   # required for BaseSettings in config.py
python-dotenv>=1.0.0
python-multipart>=0.0.9   # required for UploadFile in routes.py

# LangGraph for agentic workflow
langgraph>=0.2.0

# Logging
loguru>=0.7.2

# Testing
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-mock>=3.12.0

# Type hints
typing-extensions>=4.9.0

groq